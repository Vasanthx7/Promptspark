import React from "react";

const Profile = ({ name, desc, data, handleEdit, handleDelete }) => {
  return (
    <section className="">
      <h1>{name} Profile</h1>
    </section>
  );
};

export default Profile;
